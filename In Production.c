#include <stdio.h>

int main(void){

    float a, b, product;

    printf("Enter the first number: ");
    scanf("%f", &a);

    printf("Enter the second number: ");
    scanf("%f", &b);

    printf("Product = %.2f", product = a*b);

    return 0;
}
